# Banco de dados — PostgreSQL (Neon, servidor)
ADIADO por orientação do professor — só interface depois da base.
- `schema/` definição de tabelas e índices; `migrations/` versionadas e reversíveis; `seeds/` dados iniciais.
- Executado apenas no servidor; nunca importado pelo frontend, pelo domínio ou pelos casos de uso.
- Tokens e segredos nunca ficam em texto puro; arquivos binários permanecem no Drive.
